# Canonical URL handling is intentionally centralized in src/index.js.
# Cloudflare Workers Static Assets redirects run before asset responses and can
# conflict with extensionless directory routes. Keep this file rule-free so the
# Worker can serve exact index.html assets with assets.html_handling = "none".
